filters:
  and:
    - file.folder != "Templates"
display:
  file.name: Name
  property.entry: Entry
  property.status: Status
  property.author: Author
  property.year: Year
  property.format: Format
  property.genres: Genres
  property.lists: Lists
  property.date: Finished
views:
  - type: table
    name: Series
    filters:
      and:
        - contains(property.tags, "book-list")
        - file.folder != "Templates"
    order:
      - file.name
      - author
    limit: 100
    columnSize:
      file.name: 730
      property.author: 300
  - type: table
    name: Entries
    filters:
      and:
        - contains(property.tags, "book")
        - or:
            - contains(concat(property.lists), this.file.name)
            - contains(concat(property.author), this.file.name)
    group_by: property.status
    order:
      - entry
      - status
      - file.name
      - author
      - year
      - format
    columnSize:
      property.status: 150
      file.name: 300
      property.author: 300
      property.year: 100
      property.format: 300
    sort:
      - column: property.entry
        direction: ASC
      - column: property.year
        direction: ASC
